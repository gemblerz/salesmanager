# Sales Manager application arguments for install.sh
#
# Deployment mode:
# - local  : site password is disabled
# - public : site password is enabled
deployment_type: public

# Site access password used only when deployment_type is public.
site_password: salesmanager
